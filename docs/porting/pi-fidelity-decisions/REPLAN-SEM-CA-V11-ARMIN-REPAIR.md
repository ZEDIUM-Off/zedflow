# Replan semantic Coding-agent Armin repair

`SEM-CA-V11-VALIDATE` reported the frozen manifest's deterministic failure: `crates/zedflow-coding-agent/src/modes/interactive/components/armin.rs:7` is a marker-only mapped source target.

The terminal validator is removed. `REPLAN-SEM-CA-V11-ARMIN-REPAIR` attaches directly to its already-satisfied predecessor, `SEM-CA-V10-TEST-BATCH-5`, and owns only the diagnosed target. It must replace the marker with the frozen Pi Armin bitmap rendering and animation behavior without dispositions or sibling-component scope. `REPLAN-SEM-CA-V11-VALIDATE-R1` reruns the same package gates, and `SEM-CA-V12-FIDELITY` now depends on that fresh validator.
