# Coding-agent install-method acceptance and global package-root wave

## Accepted install-method primitives wave

At base `6daf9757347852be38f86fd4ada052a6e2bd18f3`, `CA-RV-FID-V14-R4` accepted the repaired coding-agent install-method primitives wave after workspace validation. The accepted wave covers install-method detection and pure update-instruction formatting, including inferred npm prefixes and exact frozen Pi JavaScript whitespace display quoting. The worktree and frozen `references/pi` gitlink were clean at checkpoint start.

## Next bounded wave

The next wave stays inside `zedflow-coding-agent` and ports only command-output reading and global package-root discovery from frozen `config.ts`.

A workspace validator and fresh Pi-fidelity reviewer gate the next checkpoint. Path comparison, entrypoint discovery, path writability checks, managed-install classification, settings loading, compaction execution, session runtime, extensions, package management, project trust, RPC, CLI, interactive TUI, and global HTTP dispatcher/fetch installation remain unassigned.
