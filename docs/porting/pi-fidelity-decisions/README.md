# Pi fidelity decisions

Each completed unit records observable Pi behavior, Rust implementation, alternatives considered for any crate choice, exact validation SHA, and unresolved blocker. This directory is append-only evidence for final fidelity review; it does not authorize updating `references/pi`.
