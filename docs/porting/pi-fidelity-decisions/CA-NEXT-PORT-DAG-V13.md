# Coding-agent global package-root acceptance and path-comparison wave

## Accepted global package-root primitives wave

At base `2b6dfba6323d6faf06af012ea9be5206ac7126fb`, `CA-RV-FID-V15-R1` accepted the repaired coding-agent global package-root discovery primitives wave after workspace validation. The accepted wave covers command-output reading and npm, pnpm, yarn, and Bun global package-root discovery, including the exact frozen Pi pnpm global path shape. The worktree and frozen `references/pi` gitlink were clean at checkpoint start.

## Next bounded wave

The next wave stays inside `zedflow-coding-agent` and ports only existing-path normalization and path-comparison candidate generation from frozen `config.ts`.

A workspace validator and fresh Pi-fidelity reviewer gate the next checkpoint. Entrypoint discovery, path writability checks, managed-install classification, self-update orchestration, settings loading, compaction execution, session runtime, extensions, package management, project trust, RPC, CLI, interactive TUI, and global HTTP dispatcher/fetch installation remain unassigned.
