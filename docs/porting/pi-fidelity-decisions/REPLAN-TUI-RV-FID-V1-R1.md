# REPLAN-TUI-RV-FID-V1-R1

The fidelity review found that combining marks are not included in word-like segments. Remove the blocked reviewer, repair the TUI primitive and regression tests from its satisfied dependency `TUI-V1`, then run a fresh validator and fidelity reviewer before the existing continuation checkpoint.
