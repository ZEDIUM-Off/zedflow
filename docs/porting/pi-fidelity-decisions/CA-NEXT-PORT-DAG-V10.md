# Coding-agent config/auth-guidance acceptance and derived-path wave

## Accepted config and authentication-guidance wave

At base `6feae0e830e3b9239f0dd066b5f92ddeee6afa52`, `CA-RV-FID-V12-R7` accepted the repaired coding-agent config and authentication-guidance primitives wave after mechanical and focused validation. The accepted wave covers package identity, documentation and agent-directory paths, provider login guidance, empty environment values, package-directory normalization, platform home lookup, and relative package asset paths. The worktree and frozen `references/pi` gitlink were clean at checkpoint start.

## Next bounded wave

The next wave stays inside `zedflow-coding-agent` and ports only the remaining package asset and agent-directory-derived path primitives from frozen `config.ts`, plus share-viewer URL formatting.

A workspace validator and fresh Pi-fidelity reviewer gate the next checkpoint. Install and update detection, settings loading, compaction execution, session runtime, extensions, package management, project trust, RPC, CLI, interactive TUI, and global HTTP dispatcher/fetch installation remain unassigned.
