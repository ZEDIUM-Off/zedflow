# Coding-agent path-comparison acceptance and entrypoint-discovery wave

## Accepted path-comparison primitives wave

At base `12771f99fd6092eaab95d68d07425a90cf88b044`, `CA-RV-FID-V16` accepted the coding-agent existing-path normalization and path-comparison candidate primitives wave after workspace validation. The accepted wave preserves frozen Pi behavior for missing paths, optional symlink resolution, Windows case normalization, and duplicate candidate removal. The worktree and frozen `references/pi` gitlink were clean at checkpoint start.

## Next bounded wave

The next wave stays inside `zedflow-coding-agent` and ports only entrypoint package-directory discovery from frozen `config.ts`.

A workspace validator and fresh Pi-fidelity reviewer gate the next checkpoint. Path writability checks, managed-install classification, self-update orchestration, settings loading, compaction execution, session runtime, extensions, package management, project trust, RPC, CLI, interactive TUI, and global HTTP dispatcher/fetch installation remain unassigned.
