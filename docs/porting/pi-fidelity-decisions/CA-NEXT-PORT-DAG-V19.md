# Remaining coding-agent rows before TUI and orchestrator

## Accepted utility-complements wave

At this checkpoint, `CA-RV-FID-V20-R2-V2` has accepted the repaired coding-agent utility-complements wave. The global Pi port remains incomplete: most coding-agent runtime, CLI/RPC, TUI, and orchestrator manifest rows are still unported.

## Next bounded sequence

The DAG now represents the remaining coding-agent manifest rows in dependency order: residual utility modules, core/session runtime, resource and extension loading, CLI/RPC modes, and interactive integration. A package validator and fresh Pi-fidelity reviewer gate those rows before a successor checkpoint. TUI and orchestrator rows remain downstream of all coding-agent rows. Final workspace validation plus independent fidelity and Rust reviews remain explicit terminal gates; no checkpoint before those gates is treated as completion.
