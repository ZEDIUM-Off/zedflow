# Coding-agent model-resolution acceptance and config/auth-guidance wave

## Accepted model-resolution wave

At base `f6d4be5a05c1ff9e89a1db66845c33ef802e027a`, `CA-RV-FID-V11-R2` accepted the repaired coding-agent model-resolution primitives wave after mechanical and focused validation. The accepted wave covers model reference matching, pattern parsing, scoped-model resolution, diagnostics, minimatch separator behavior, and leading-`!` negation behavior. The worktree and frozen `references/pi` gitlink were clean at checkpoint start.

## Next bounded wave

The next wave stays inside `zedflow-coding-agent` and ports only package identity, documentation and agent-directory path primitives from frozen `config.ts`, together with the provider login guidance that consumes the documentation path.

A workspace validator and fresh Pi-fidelity reviewer gate the next checkpoint. Install and update detection, settings, compaction execution, session runtime, extensions, package management, project trust, RPC, CLI, interactive TUI, and global HTTP dispatcher/fetch installation remain unassigned.
