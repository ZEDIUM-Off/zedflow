# REPLAN-TUI-RV-FID-V2

The TUI fidelity reviewer found that the manifest wave still exposes only keys/primitives while Pi's `tui.ts` runtime, container behavior, overlays, rendering, input, and cursor behavior remain absent. The originating reviewer is removed and replaced by bounded runtime, key-decoding, and focused-test writers, followed by fresh validation and review. Downstream orchestration is reconnected to the fresh reviewer.
