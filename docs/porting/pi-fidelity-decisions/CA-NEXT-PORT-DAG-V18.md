# Coding-agent utility-complements wave after utility-substrate acceptance

## Accepted utility-substrate wave

At the preceding checkpoint, `CA-RV-FID-V18-R3` accepted the bounded coding-agent utility-substrate wave after the focused Git URL repairs. The global Pi port remains incomplete: coding-agent, TUI, and orchestrator still have unported manifest rows. This checkpoint changes only the port plan and state; it does not alter product Rust.

## Next bounded wave

The next wave stays inside `zedflow-coding-agent` and ports the remaining dependency-light utility complements from frozen Pi: deprecation and changelog helpers, filesystem watching, browser opening, clipboard capability wrappers, syntax-highlighting support, and version-check helpers. It must not begin package/resource loading, settings or session runtime, extensions, project trust, RPC, CLI startup, interactive TUI, or orchestrator work.

A coding-agent workspace validator and fresh Pi-fidelity reviewer gate the wave. Remaining coding-agent rows stay ahead of orchestrator. Every non-final wave requires a successor `NEXT-PORT-DAG` checkpoint; completion still requires every manifest row plus final workspace validation and fidelity/Rust reviews.
