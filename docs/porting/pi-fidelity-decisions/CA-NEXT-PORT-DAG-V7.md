# Coding-agent seventh-wave acceptance and eighth wave

## Accepted seventh wave

At base `02ab46d387c7e0831b28e18c5a2407d25b0da0e8`, `CA-RV-FID-V7-R1` accepted the repaired seventh coding-agent wave after mechanical and focused validation. The accepted wave covers credential storage, runtime API-key overrides, environment and fallback lookup, OAuth credential refresh, locked JSON persistence, and persistence-error reporting. The worktree and frozen `references/pi` gitlink were clean at checkpoint start.

## Next bounded wave

The next wave stays inside `zedflow-coding-agent` and ports only built-in and custom model registration, provider configuration and validation, and authentication resolution. It does not begin model selection/resolution or session integration.

A workspace validator and fresh Pi-fidelity reviewer gate the next checkpoint. Model selection/resolution, settings, compaction execution, session runtime, extensions, package management, RPC, CLI, interactive TUI, and global HTTP dispatcher/fetch installation remain unassigned.
