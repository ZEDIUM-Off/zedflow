# Coding-agent derived config paths acceptance and install-method wave

## Accepted derived config path wave

At base `acb67bfa64cb2df82faf242c97398f93d0b115cb`, `CA-RV-FID-V13-R1` accepted the repaired coding-agent derived config path primitives wave after workspace validation. The accepted wave covers package assets, agent-directory-derived paths, share-viewer URL formatting, and bundled-binary asset resolution beside the executable. The worktree and frozen `references/pi` gitlink were clean at checkpoint start.

## Next bounded wave

The next wave stays inside `zedflow-coding-agent` and ports only install-method detection and pure update-instruction formatting from frozen `config.ts`.

A workspace validator and fresh Pi-fidelity reviewer gate the next checkpoint. Global package-manager discovery, path writability checks, command execution, settings loading, compaction execution, session runtime, extensions, package management, project trust, RPC, CLI, interactive TUI, and global HTTP dispatcher/fetch installation remain unassigned.
