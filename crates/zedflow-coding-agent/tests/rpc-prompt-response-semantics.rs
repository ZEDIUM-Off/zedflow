//! Pi coding-agent test manifest entry: `tests/rpc-prompt-response-semantics.rs`.
//!
//! The deterministic Rust contract is owned by the package modules; retain
//! this integration-test target so the frozen package layout stays one-to-one.

#[allow(dead_code)]
pub const TEST_PATH: &str = "tests/rpc-prompt-response-semantics.rs";
